// The active compact v2 course. The repo-learning-builder skill replaces
// course.json per project; the app template ships the ClaimFarm demo course.
import course from './course.json';

export default course;
